apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-pdf-exporter.fullname" . }}
  labels:
    {{- include "grafana-pdf-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-pdf-exporter.selectorLabels" . | nindent 4 }}
